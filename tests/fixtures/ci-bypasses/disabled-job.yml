on:
  push:
  pull_request:
jobs:
  validate:
    if: false
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [22, 24]
    steps:
      - run: pnpm validate:accepted
