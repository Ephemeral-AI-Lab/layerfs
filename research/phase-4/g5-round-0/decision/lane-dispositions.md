# G5-1 lane dispositions

Exactly one disposition is selected per lane.

## G5-A — `RETAIN_FULL_REOPEN_AUTHENTICATION`

The current receipt and same-open permit are sound inside their proved mutation/open boundary; no non-replayable external authority exists across reopen. Xet authenticates immutable content and remote access/retry state, not freshness of a mutable local store. `AUTHORITY_EXPERIMENT_READY` is rejected.

## G5-B — `RETAIN_K64_F64`

Final Canonical-v2 controls are 196,375/100,763/5,334 operation-counter bytes. Exact Xet 3–9 fails the 105% live gate even under all-nine grouping. CD32–64 is an informative future metadata-only shadow but remains suffix-linear under adversarial cut streams and is not a qualifying persistent profile.

## G5-C — `H11_REVISE_EXACT_BLOCKER`

H11 v2's sealed analyzers passed their selected identity/work/storage/latency checks, and a post-terminal hash-bound audit confirmed parity for emitted fields the analyzers omitted. However, the harness loaded a 459,443-byte manifest and a 1,001-record uncharged expectation vector, built uncharged current/retained `BTreeSet`s, retained uncharged timing/report buffers, omitted reachability Q from `q_max`, and emitted literal `q_current:0`. This violates the hard whole-process Q rule. Timed reopen also has incomplete SQL counters and applies the 1,500-page setting only after open. V2 is diagnostic evidence, not a qualifying H11 PASS.

## G5-D — `RETAIN_CURRENT_SQLITE_PROFILE`

No directly observed controlled-cold or physical-byte-I/O evidence exists. Current profile and durability semantics remain protected. A benchmark-private VFS/locality characterization is candidate-order research, not an admitted profile change.

## Single G5-2 starting action

**Freeze the smallest corrected H11 whole-harness Q/custody protocol before any full G5-C history/concurrency/GC gate.** Do not start that action inside G5-1.
