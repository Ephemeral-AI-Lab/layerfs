# Phase 1 admission cohort on the 8-CPU Docker VM

The user explicitly authorized proceeding with the 8-CPU Docker VM and
completing Phase 1. The VM was changed from four to eight CPUs by a separate
implementation task with prior user authorization. Its memory setting remains
4096 MiB. The Phase 1 benchmark containers retain their existing **2-CPU and
2-GiB limits**; every other fixture, oracle, deadline and resource gate remains
unchanged.

The 373 completed active performance samples from source `6c54f8d7` remain
authentic observations of the four-CPU VM environment. They are preserved with
their original source and environment identities. They must not be paired with
eight-CPU verification evidence or described as measurements of the new VM.

To satisfy the existing environment-matching admission gate, collect the active
performance cohort on the current eight-CPU VM and use matching full proofs.
This collection is required by the authorized environment migration, not by
verifier-only code changes. Keep the previous observations in the report's
historical source inventory, without labelling their actual outcomes failed.

Reuse the 37 completed full proofs already collected on the eight-CPU VM when
their fixture, schedule, source and required checks remain compatible. The
older one-MiB payload proof remains four-CPU evidence; obtain the small matching
eight-CPU proof for the new timed cohort. The completed 600-second endurance
proof remains a separately identified standalone proof at its actual source
and environment, without an eight-CPU timing claim or an unnecessary rerun.

The persistent Phase 1 runtime suppressions still apply before preparation,
including the cumulative 15-second limit on new performance samples. Retain
all suppressed definitions, historical failures and incomplete attempts. A
fast-iteration verification result cannot fill an exhaustive verification slot.

Freeze the VM configuration during the collection and coordinate exclusive
measurement windows with other tasks. Do not pool four-CPU and eight-CPU
performance distributions, silently normalize their identities, weaken the
matching gate or stop unrelated containers.

At the migration checkpoint, 344 new full checks remain for the current timed
cohort and required standalone proofs: the previously outstanding 343 plus the
small payload proof that needs the matching environment. The 373 new active
performance samples are a separate lane and are collected before resuming the
remaining exhaustive verification campaign. Counts may decrease only through
the already authorized persistent runtime-suppression policy, with explicit
retained outcomes and scope accounting.
