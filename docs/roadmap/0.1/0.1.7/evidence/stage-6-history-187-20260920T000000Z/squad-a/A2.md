# A2 — pointer

This squad's report is [`A2-producer-callgraph.md`](A2-producer-callgraph.md) (the
`squad-a/A2.md` name in the shared deliverable template).

Headline, in one paragraph: in v0.1.7 core the history lane's whole-file objects come from
`layerfs-content/src/file/content.rs:207 construct_bytes`, which attaches **no** advisory
predecessor; the only C1 site that attaches one to a `WholeFile` object is
`file/edit/apply.rs:121`, behind `apply_edits`, which the lane never calls. Measured on the
existing baseline Store: **0 of 18,344** whole-file delta bases are cross-save, against
**917 / 4,461,895 B** cross-save `InodeLeaf` bases from the one producer the lane *does*
reach (`filesystem/sorted/page.rs:383`). v0.1.6 declares the previous commit's content root
for the same path at `crates/layerfs-workspace/src/changes.rs:1524-1527`, records it at
`crates/layerfs-layerstack-store/src/objects.rs:3318` and consumes it at
`objects/admission.rs:441-450`.
