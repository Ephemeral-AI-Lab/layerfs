# A4 — pointer

Squad A4's deliverable is **[A4-v016-reconstruction.md](A4-v016-reconstruction.md)** in this
directory. The brief named that file explicitly; this file exists only so a reader following the
campaign's `A4.md` convention lands in the right place.

One-line result: v0.1.6's `history-stride10` Store is **retained**
(`benchmark-results/repository-history/stride-10/deepseek-stride10/host-runtime/store.sqlite`,
sha256 `80c2b10a…` = the verification manifest's), so its per-bucket table is **exact, not a
bound** — 35,904 whole-file records with a declared delta base (285,486,222 B canonical →
16,310,397 B, 17.503×) against 8,237 without (62,974,073 B → 22,672,881 B, 2.778×), total
49,315,940 B apparent / 49,344,512 B allocated, both reconciled with **zero** residual.
**31,108 of those records name a base in a strictly earlier pack (an earlier commit), so 49.3 MB
is unreachable without cross-commit delta bases by ≥ 2.46×.**
