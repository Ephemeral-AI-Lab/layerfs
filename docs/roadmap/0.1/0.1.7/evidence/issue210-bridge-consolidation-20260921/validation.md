# Bridge consolidation verification

> **Status:** Dated planning checkpoint; not release evidence or a product contract.

Verified product/test commit: `7a6db4c2d7f4b9182ea7b0c923eabcafe96dbb0c`.
Parent: `a4a144af8af9b46c3ce3100047466f4817332808`. See the
[implementation record](implementation.md) and [independent review](independent-review.md).
The evidence/source-pin follow-up changes no product or test inputs.

## Change and LOC

Four bridge source files share existing optional-field primitives and page
loops, construct decoded records directly, and remove redundant matching. No
public DTO, wire tag/version, dependency, record minimum, byte/count cap, error
class, C5 operation or C1/C2 behavior changes. The page allocation/check order
and per-record validators are preserved. There is no new product file.

| Scope | Before | After | Delta |
| --- | ---: | ---: | ---: |
| Bridge | 3734 | 3658 | -76 |
| Core | 30927 | 30851 | -76 |
| Reference | 65417 | 65417 | +0 |
| Combined | 96344 | 96268 | -76 |

The four-file deltas are metadata -23, response -51, client -3 and
history_failure +1. Pair 2's total addition from a02168ad is now 5457, of which
1750 is in the bridge. This is a source-size reduction, not a performance claim.

The code commit records `Production LOC: 96344 -> 96268 (delta -76)`.
The evidence-only follow-up records `Production LOC: 96268 -> 96268 (delta +0)`:
core 30851 unchanged; reference 65417 unchanged. Each comparison uses the same
`tools/production_loc.py --root <exact git-archive snapshot> --detail --json`
counter for first parent and final staged source, with committed source equality
confirmed. Tests, fixtures, docs, tooling, manifests, artifacts, comments/blanks
and legacy inline tests are excluded; Rust implementation and runtime SQL count.
Counter SHA-256: `c6e853c2280e2ee96caa6cafff4b221fa9201e1f5bf40d12e8251f70387210fc`.
Raw counts are retained below; no source scope was moved or retired.

## Verification at 7a6db4c2

All commands below ran from `core/`, with owned target directory
`/Users/yifanxu/.codex/worktrees/b995/layerfs/target/remediation-core` and
`LAYERFS_CONSTRUCTION_WORKERS=1`. Exact commands are in [commands.json](raw/commands.json).

| Check | Result | Raw output |
| --- | --- | --- |
| `cargo +1.85.1 test --offline --locked --workspace` | 603 passed, 105 result groups, no failures | [tests](raw/cargo-test.log) |
| `cargo +1.85.1 build --offline --locked --workspace --examples` | PASS | [examples](raw/cargo-examples.log) |
| `cargo +1.85.1 fmt --manifest-path Cargo.toml --all -- --check` | PASS | [fmt](raw/cargo-fmt.log) |
| `cargo +1.85.1 clippy --offline --locked --workspace --all-targets -- -D warnings` | PASS | [Clippy](raw/cargo-clippy.log) |
| `cargo +1.85.1 build --offline --locked -p layerfs-history --no-default-features` | PASS, contract separation only | [no-native](raw/cargo-no-native.log) |
| `python3 tools/check_product_boundary.py` | PASS, 194 production files | [boundary](raw/boundary.log) |
| `python3 -m unittest discover -s tools -p 'test_*.py'` | PASS, six tests | [self-tests](raw/python-tests.log) |
| `cargo +1.85.1 build --offline --locked --workspace --bins` | PASS | [bins](raw/cargo-bins.log) |
| Fresh production host history route | 14 cases PASS | [receipt](raw/host-route.json) |
| Fresh matched ARM64 Linux Docker daemon to macOS host service route | 14 cases PASS | [receipt](raw/linux-route.json) |

The external golden fixture contains 46 encodings generated by the unchanged
a4a144af production codecs before edits: queries/commands, results and typed
failures. Its new assertion passed, along with existing malformed/truncated,
minimum/maximum width, exact/over-budget, legacy and mutation-delivery tests.
[Baseline capture](raw/baseline-wire.log), [focused tests](raw/focused.log).
This protects byte identity against an encoder/decoder changing together.

The initial pre-commit Clippy run rejected `format!` inside iterator collection
in the new test. It was replaced with `write!` into one String; no product change
was needed. [Original failure](raw/clippy-precommit.log) and
[corrected run](raw/clippy-precommit-corrected.log) are both retained. No failure
was erased or relabeled. Original raw output whitespace is preserved.

An independent read-only source review found no actionable issue. The committed
production diff matches its SHA-256
`8e8106eace053fd27ffa2b2e1f50cea13b0035c878df5e1e1eed780fb9797ffe`.
The reviewer inspected the fixture but did not independently run tests or
regenerate it; execution evidence above belongs to this task.

[Source/binary identities](raw/identities.json), [staged/committed tree](raw/source.json),
[Linux build](raw/linux-build.log), and [new image identity](raw/image.json)
record the actual artifacts. The Linux daemon was rebuilt using offline/locked
`cargo +1.85.1 zigbuild --target aarch64-unknown-linux-musl -p layerfs-daemon` in
owned `target/remediation-linux`, then packaged using the existing scratch
Dockerfile. Both route output directories were fresh. Dependencies and Cargo
build caches were reused outside functional operations; old images/receipts were
not promoted to new evidence. Source was clean at the checks. No test container
remained after the Linux route.

No benchmark, performance claim, aggregate gate, preflight, CI, new dependency,
third-party change, worker/cache/timeout adjustment, retry or durability change.
The prior [H-case qualification record](../issue210-remediation-20260921/validation.md)
still applies: H04 history overlap/reverse failure, H06 independent-stack upload
overlap, H08 real boundary/identical-root failures and full H14 history-consumer
substitution remain unverified. Those schedules and the unchanged C1/C2-only
substitution harness were not rerun for this internal codec refactor. No merge,
release or issue closure is implied.
