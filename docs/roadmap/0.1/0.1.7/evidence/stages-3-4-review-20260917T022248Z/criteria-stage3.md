### 4.1 Stage 3 (#168)

| # | Requirement | Source | Evidence | Status |
| --- | --- | --- | --- | --- |
| A1 | Exact decoded bytes/identity, candidate decisions, chain/dependency/chronology checks, both delta roles preserved | `delta/select.rs`, `delta/read.rs`, `cas/dependencies.rs` | `delta_payload` 15/15, `delta_chains` 9/9, `cas_roundtrip` 6/6 (exit 0) | **PASS** |
| A2 | Missing optional candidates are normal policy; failed acquisition/codec/SQL fail, never recover through FULL | `delta/select.rs`, `full.rs` | `delta_payload::an_absent_or_ineligible_candidate_selects_full`; `w1/w1-fails-without-fix.log` shows 3 FAILED without the fix | **PASS** |
| A3 | Pooling retains exact window, ordinal choice, authenticated equality, bounded memory; catalogue work attributed | `encoding/pool/*`, `sqlite/pool.rs` | `metadata_pool` 15/15, `metadata_pool_index` 5/5, `metadata_window` 2/2, `metadata_fingerprint_collision` 2/2, `metadata_chain` 2/2 | **PASS** (with F-10, F-19a) |
| A4 | Stable locators, packing density, supported singletons and configuration overrides qualified | `pack/*`, `policy.rs` | `pack_locator` 8/8, `physical_formats` 6/6, `policy_capacity` 9/9; `--case small-to-large --threshold-bytes 1048576` exit 0 | **PASS** |
| A5 | Base corruption, depth/work boundaries, low/high reuse, cold/reopen, error cases covered | tests | `delta_chains`, `metadata_pool`, `visibility` 7/7, `persistence_failure` 7/7, `cas_reuse` 8/8 | **PASS** (with the UNRUN 8 MiB refusal, see `7) |
| A6 | Independent timers and complete save/read evidence include all real acquisition/encoding/packing/SQL work | `measure_edits.rs`, `measure_pooled.rs`, telemetry | 21 timing arms; **`e1c-pooled-512` is clipped: 42.54 % attributed / 57.46 % unattributed**; all arms ran in the debug profile | **INCOMPLETE** |
| A7 | Storage/index/column footprint, memory and speed claims measured under approved cases; no invented win from smaller delta frames | — | **no artifact**: `verification.md:109`; matched ledger `:83–86`; gates G13/G15 | **INCOMPLETE** (owner-waived) |
| E1 | Checked canonical inode-leaf/value grammar, exact value-group body/digest/compression, ordinal/catalogue persistence, pooled FULL and authenticated reopen | `object/inode_leaf.rs`, `pool/*` | `inode_leaf` 6/6, `metadata_pool` 15/15; 73-byte value equality, 165 values per group, digest taken over the uncompressed body before compression | **PASS** |
| E2 | Bounded Store-owned ordered index, same reference window/reset, exact-equality/smallest-ordinal, pending/reopen/failure semantics | `pool/index.rs`, 131 072-entry window | `metadata_pool_index` 5/5 (including the searched 64-bit collision pair), `metadata_window` 2/2 | **PASS** |
| E3 | Pooled COPY/INSERT DELTA with its own work/depth limits, intermediate authentication, atomic persistence, one owned cleanup | `pool/delta.rs`, `pool/read.rs`, `sqlite/cleanup.rs` | `metadata_pool` delta cases, `visibility`, `persistence_failure` | **PASS** |
| E-extra | Same-save delta-base eligibility and admitted-FULL cache deviations resolved or documented with evidence | `delta/candidates.rs`, `select.rs` | `delta_payload::the_chunk_lane_honours_its_own_depth_cap` plus 3 cases in W1; `stages-3-4-report.md `7.7` records the cache as a bounded heuristic | **PASS** (documented; candidate loss is bounded and is not a correctness risk) |
| E-qual | Qualify real pooling/payload/pack operations, retained footprint, simultaneous index/codec/SQL memory | `examples/memory_ledger.rs`, `w7` | single sample, **debug** profile, receipt from `aa4b5a9e4` rather than HEAD; fixture labelled "the E1b shape" but is 24 leaves / 2 400 values against E1b's 128 leaves / 227 entries | **INCOMPLETE** |
| C1-iso | C1 logical edits run with supplied I/O and no C2/SQLite dependency | `content/Cargo.toml`, `content/src/lib.rs:16` | dependencies are `blake3` + `layerfs-telemetry` only; `#![forbid(unsafe_code)]`; `measure_edits --mode c1` prints "no database, pack, Store or file is opened in this mode", exit 0 | **PASS** |
| C2-iso | C2 saves supplied objects without C1 file construction | `cas/*` | no `construct_file`/`apply_edits`/`build_streaming` call in C2 src; `measure_edits --mode c2` exit 0 ("no C1 file construction … timed") | **PASS** |
| Config | Defaults 128 KiB/8/4; accepted non-default cutoffs and depths; 256-KiB and 1-MiB cases; derived capacities; persisted reopen agreement; explicit rejection; no clamping | `content/policy.rs:81–102,120–159`, `storage/policy.rs:188–210,296–322`, `sql/schema.sql` | `policy_capacity` 9/9; `edit_transitions::exact_boundaries_at_every_accepted_cutoff`; accepted cutoffs are the four powers of two in [128 KiB, 1 MiB]; depths 0…50 for all three roles; `user_version` 4 persisted and re-validated on open | **PASS** (with F-17) |
| Depth | Separate WHOLE_FILE/CHUNK/pooled-metadata depth and work bounds; history longer than the cap selectable; raising depth does not raise budgets | `policy.rs`, `delta/read.rs:139–159`, `pool/read.rs` | `delta_chains` 9/9 (default and changed caps, first excluded edge), `delta_payload::a_fifty_link_chunk_chain_is_admitted_and_read`; chain bytes are separate fixed constants | **PASS** |
| Payload | WHOLE_FILE and CHUNK FULL/PREFIX, exact reuse first, explicit/cache/first-candidate ordering, framed cost and FULL-based grouping, at most one trial | `delta/*` | `delta_payload` 15/15; `--mode c2 --case small` prints `prefix records 1 full records 1 trials 1` | **PASS** |
| Pack/read | Placement before one selected assembly, stable locators, bounded grouped acquisition and selected-encoding singleton, explicit dispatch, no trial decoder or RAW switch after error | `pack/placement.rs`, `pack/layout.rs`, `encoding/decode.rs` | `pack_locator` 8/8, `physical_formats` 6/6 (versions 0/3/5/8/99 rejected, no probing) | **PASS** (with F-11, F-18) |
| Storage-reg | Duplicates within/across waves, unfinished-group owner reads, private early commits, retained watermark, failed-save cleanup, previously successful objects intact | `cas_reuse`, `visibility`, `persistence_failure` | 8/8, 7/7, 7/7 exit 0; the `acefc3179` watermark fix and the `30f5d0633` pending-group fix are preserved | **PASS** |

