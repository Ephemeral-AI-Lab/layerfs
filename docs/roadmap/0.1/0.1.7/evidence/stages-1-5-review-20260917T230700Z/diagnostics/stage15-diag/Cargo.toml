[package]
name = "stage15-diag"
version = "0.0.0"
edition = "2021"
publish = false

# Standalone: this diagnostic client is not a member of any product workspace.
[workspace]

# External review diagnostic client. Public API of the reviewed core only; no
# product source is modified, included or recompiled from here.

[dependencies]
layerfs-content = { path = "../../../../../../../../core/crates/layerfs-content" }
