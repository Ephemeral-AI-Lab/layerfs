Starting the authorized live group-compression experiment from PR #198: **GROUP_LEVEL 19 versus 1 only**. Payload level 3, encode workspace, workers, window/integrity checks, cache capacities and harness stay fixed. One current stride10 sample per arm, separate correctness verification, then stride3 confirmation only if worthwhile. One second is worthwhile; small allocation overages are acceptable for good time reduction, with actual sizes reported.

The historical 1.011835/1.425492-second codec CPU figures are motivation only. This pair will measure current operation/save time, whole-invocation CPU and storage; codec-only CPU remains unmeasured without an existing isolated counter. No default is accepted before results. Prior optimizations stay intact; raw outputs remain append-only, cache admission INELIGIBLE and O3 gaps visible.
