Starting the agreed narrow ROI step from PR #195. Existing release symbols support native stack profiling, so **no additional production telemetry or rebuild** is needed for attribution. One labelled stride10 run will be sampled at 5 ms under the shared measurement lock; profile times are not speed claims.

Source review found that pack BLOB lookup and pooled catalogue lookup prepare fixed SQL repeatedly, while neighboring queries already use the existing prepared-statement cache. Only preparation/parser work beneath a named helper will justify a local statement-reuse change. If the remaining cost requires broader cache lifetimes/accounting, or attribution is unclear, stop without a product optimization. At most one local treatment, then fresh unprofiled matched stride10/stride3 evidence if warranted. No capacity, value-cache, worker, codec or format change. Baseline production LOC85,723; attribution adds0.
