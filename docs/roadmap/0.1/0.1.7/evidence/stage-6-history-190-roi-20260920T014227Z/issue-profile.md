Native attribution completed with **0 added production LOC**. The main-thread profile has7,307 stack-residence samples, including1,759 under StoreProvider. Independent parsing found catalogue-query preparation204, execution312, reset87, finalization/drop12, row decode5, other4. These are sample estimates, not exact CPU time. Reset remains required and is not counted as removable.

The evidence justifies one small experiment: use the **existing bounded prepared-statement cache for `group_for` only**. Keep the SQL/results/validation unchanged; no pack lookup change or broader data cache. An external authorizer test will prove preparations fall from two to one across repeated calls while an intervening row update remains visible. If correctness or the matched result fails to justify it, stop and retain the rejection. The profiled wall is not used in the speed comparison.
