**Rejected the new-row zero-count direction under the multi-second threshold.** The entire unchanged zero-count phase measured **1,699,077,376 ns** across 16 updates (stride10 has one initial build plus 16 updates), below the prospectively frozen **2,000,000,000 ns** bar. The removable new-row portion is only a subset. This is a direct-cost screen for this sample, not a portable timing limit or a claim about unmeasured indirect cache effects.

The diagnostic operation was22,615,178,250 ns; complete command42,162,170,750 ns;273 timer nodes, within the unchanged1024 cap. Temporary source patch, binary, identity and per-state receipt are archived. **The two-line timer wrapper has been removed; production source exactly matches the merged baseline.** No candidate implementation, structural-test expansion, stride3 run or full product test sweep will be spent on this direction.

PRs #194/#195/#196 are retained unchanged. Final product LOC remains85,722 (delta0); the temporary instrumentation was+2 and is not retained. One separate sampled verifier and independent arithmetic review finish evidence custody. The small signature/pack-statement/temporary-run ideas remain excluded. No other reviewed low-complexity direction is currently demonstrated to offer a multi-second stride10 reduction.
