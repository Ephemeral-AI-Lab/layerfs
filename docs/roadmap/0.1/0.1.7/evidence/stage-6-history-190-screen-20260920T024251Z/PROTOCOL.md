# Multi-second screen for zero-count new-row filtering

> Status: Research; informative and not a product contract.

Owner instruction: retain PR194/195/196 and only invest in directions with multi-second reductions. Prospective investment bar: at least2,000,000,000 ns reduction in stride10 measured operation; stride3 is confirmation, not a way around the stride10 bar. Do not lower the threshold after results. Signature, pack-statement and temporary-run micro-optimizations are excluded.

Base ca13fb1709250be64711a9431146e1aceb390955. First cheap screen: one existing coarse phase named zero_count around the unchanged zero_count_serials call. No algorithm, arguments, work counters, defaults or interfaces change. At most16 additional timer nodes in stride10 (52 in stride3); existing1024 cap unchanged. One fresh stride10 diagnostic prices the WHOLE phase. If sum<2s, even deleting the entire phase cannot meet the bar in that run: reject this direction immediately, archive the patch/binary/receipt and restore product source. No candidate implementation, full test sweep or stride3 measurement follows that rejection.

If whole phase>=2s, that does not authorize implementing the filter: first prove substantial removable provider pages. High new IDs already stop at a branch root; mixed waves may save no pages. Re-added paths may reuse absent interior IDs, so do not assume every New is above the maximum. Use existing public CountingProvider fixtures/corpus metadata to distinguish the removable work. Only a credible >=2s direction proceeds to a matched pair and full correctness/stride3 confirmation.

Same existing immutable corpus, fresh Store/output, single construction worker, detailed phases, eight behavior switches unset. Reuse incremental locked build. Shared TMPDIR+/tmp flocks and harness O_EXCL lock; no overlapping resource commands. Preflight no named Cargo/rustc/fs-bench competitor and>=70% CPU idle. Existing120s stride10 diagnostic and60s verification hard budgets,10s verification target; no enlargement, rerun-for-better or stride1. Cache uncontrolled, no priming/purge; admissionINELIGIBLE and historyO3INCOMPLETE. Preserve deferrals/errors/misses.

The temporary timer patch is an ordinary supported phase callback, not a benchmark-specific alternate algorithm. If rejected, final production LOC remains85,722 (reference65,417/core20,305); retain temporary instrumentation LOC separately. No previous successful optimization is reverted. Archive identities/trace/patch; record measured numbers and stopping decision in issue190 and ledger.
