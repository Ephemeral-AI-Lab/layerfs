# Owner revision: one-second savings are worthwhile

> Status: Research; informative and not a product contract.

After the screen, the owner explicitly stated: "1 second is good optimization, do not dismiss it." This supersedes the prior2s investment bar for future work. The historical protocol,1.699077376s phase receipt and2s rejection are preserved; no historical receipt is relabelled. New experiment disposition `zero-count-filter-1s-v2` treats>=1,000,000,000ns stride10 operation reduction as worthwhile. The whole-phase measurement does not establish this saving.

Reuse the archived phase-only binary/receipt as the baseline, never as a new sample. Candidate must include the identical phase wrapper and unchanged harness/dependencies. First run focused public-provider structural tests for high-new, mixed/interior demands and mandatory existing-ID rejection. Implement only within-wave filtering of PendingState::New, retaining original batch boundaries, effects, limits and result order. No new caches or broader compaction. One candidate stride10 sample; confirm stride3 only if justified by actual saved work and the one-second bar. Do not repeat a sample or smooth numbers to pass.

All other frozen limits/cache/worker/lock/custody rules remain. The first baseline verifier launch did not start because of an empty private lock; keep NOT_RUN_LOCK_HELD for that launch. A later verified-safe lock recovery and fresh launch, if performed, is separately recorded. Earlier successful PR194/195/196 changes remain untouched.
