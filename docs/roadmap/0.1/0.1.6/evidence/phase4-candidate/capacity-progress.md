# Candidate capacity implementation state

This is unfinished implementation bookkeeping, not a passing capacity receipt.

Completed focused correctness evidence remains in README/verification.jsonl. `candidate-deletion-attempt05.json` is the real cleanup-source C3 PASS; attempt04 was the incorrectly selected older binary and remains explicitly invalid for that fix.

Parent approved separate canonical-construction reservations under the existing aggregate HostAdmission128MiB/512FD ceiling. Live overlay32MiB/8FD caps remain intact; they never covered old canonical machinery. Preserve old max_final_delta input allowance, canonical encoding/FULL-DELTA/CDC policies and existing default Store callers. Reduce private indexes only through existing spill behavior, below old maxima. Exact aggregate memory/disk/FD ownership and peaks remain OPEN.

Current unverified source edits:

- `objects.rs`: `DeferredObjectStore.private_owner` drops after other fields; `ObjectBuffer::limit_private_indexes(index,order)` is lower-only, empty-buffer-only, index4..64MiB, order64KiB..8MiB; both `ObjectBuffer` and `WorkspaceAdmission` have `retain_private_owner`. No caller uses these yet. `WorkspaceAdmission.private_owner` drops after its admission buffers.
- `overlay_budget.rs`: separate `ConstructionReservation` and `Budget::reserve_construction(memory,disk,files)` charge existing Scheduler/HostAdmission atomically and release through RAII. No caller uses them yet.
- NEW UNDECLARED `layerfs-layerstack-store/src/objects/scratch.rs`: charged ScratchBudget/ScratchFile/Allocation/DatabaseCharge draft. A file clone shares its actual FD and has an independent pread/pwrite cursor. Growth is reserved before writes; partial failure keeps charge; successful authoritative resize can release slack. Named allocation owners survive close/reopen independently of FD owners. SQLite charge reserves fixed-width B-tree insertion growth before transaction; still needs integration into scratch_index/seen/offsets paths and exact failure/physical-block tests.

Next executable work:

1. Finish scratch module focused tests (exact quota/+1, clone FD/cursor sharing, named close/reopen ownership, partial-write/truncate failure). Declare optional module/reexports only when source stable; serialize Cargo with root/index/HostClient agents.
2. Adapt existing Store `objects/spill.rs` with explicit optional scope in DeferredObjectStore/IdOrder/SpillableObjectSet/SpillObjects/SpillDiskIndex. Keep default callers unchanged. Named paths retain Allocation owners; account SQLite descriptors/pages, no uncounted temp fallback. Existing SQLite statements are bounded and source search found no unbounded ORDER BY/GROUP BY sorting in spill.rs.
3. Adapt shared workspace journal File type to ScratchFile (anonymous_journal remains an unscoped legacy constructor), with an optional explicit budget field on ReferenceJournal/FrontierInodes and scoped constructors for snapshot candidate task/results. Only seven anonymous_journal sites exist; avoid global/TLS context inference.
4. Add candidate_capacity helper deriving a conservative bounded canonical envelope from existing buffer/worker/FD contracts; retain Scope/host reservation in actual private output and admission through consumption/drop. Track charged and observed physical peaks distinctly; do not label reservation as RSS.
5. Enforce/journal aggregate disk and FD use before I/O; run new focused failure checks, then rerun only candidate/Store regressions exercising the new limits/ownership paths. No blanket old test rerun or benchmark campaign yet.

Ownership coordination: root approved objects.rs + new Store scratch module + objects/spill.rs, candidate_capacity.rs, snapshot_candidate.rs and narrow overlay_budget.rs. Index agent is now handling host_canonicalize and narrow Ranges/Description accessors; do not edit those. Host agent owns HostClient lifecycle gate. SnapshotCandidateFixture is now exported cfg(test) from changes.rs for canonicalization integration tests, with host/directory/store/comparison and new/inputs/publish available. Root owns commit_attempt fixture and integration.

Latest checkpoint:

- Scratch module is now DECLARED (objects::scratch, unix) and exported via Store lib. Three provider tests PASS in scratch-attempt02 after signed rusqlite conversion repair; source/result/logs are preserved. Additional named inode binding (`dev`,`ino`) and borrowed-cursor mutex implementations were added afterward and need the next affected provider verification.
- `ScratchFile` supports owned and borrowed Read/Write/Seek, FileExt pread/pwrite, Metadata/AsRawFd/set_len/sync_data, and try_clone shares one FD with an independent logical cursor. Named Allocation owners are explicit; database charges must remain until connection/path drop. Database growth uses4KiB pages, pre-reserves65pages per fixed-width row plus2 metadata pages (conservative height bound), applies max_page_count, then releases unused reservation only after authoritative metadata. `written_bytes` is explicitly pwrite-only; it does not fabricate SQLite I/O.
- Shared workspace `changes.rs` now aliases File to ScratchFile. `anonymous_journal` calls unscoped constructor; new `scoped_journal` accepts an explicit budget. ReferenceJournal and FrontierInodes have optional scratch fields and route their allocations accordingly; no snapshot caller sets these fields yet. Existing legacy defaults remain unscoped. The SnapshotCandidateFixture initial source file now explicitly uses std::fs::File::create.
- NEW UNDECLARED `candidate_capacity.rs` draft: separate96MiB canonical reservation preserves default32MiB live envelope under128MiB host;128FD allowance covers64 frontier levels/merge/task/worker/spill files; disk derives max_spool+overlay.max_scratch. It lowers private indexes to4MiB/order64KiB and retains the scratch scope in output/admission. This is not yet wired and the envelope needs actual integrated peak/capacity verification, not a claimed PASS.
- Root was told compiled sources are ready to freeze for one build of its fixed-inode/leased-file checks plus index agent's optional substitution piece-cap case. Until root releases that build, edit only undeclared files. A subsequent comment-only scratch.rs hash delta clarifies pwrite/SQLite counter domains and was explicitly reported to root.

Remaining critical integration: explicit optional scratch context and named Allocation ownership in Store spill/seen/order, SQLite descriptor/page accounting; SnapshotCandidateInputs budget acquisition + scoped task/result/reference/frontier/plan journals; prospective disk growth everywhere; real memory/FD/physical peaks and retained owner through staged admission; exact failures/quota/cleanup checks. No capacity completion claim.


2026-09-14 integration checkpoint (supersedes older undeclared/unwired entries above):

- Store optional scratch, scoped SQLite/locator/seen/order/data spill, workspace task/result/frontier/reference/V3 journals, SnapshotCandidateInputs budget and retained actual output/admission ownership are now declared and integrated. Parent runtime and coordinator fixtures use isolated LiveRuntime+HostAdmission for tests; production caps remain shared.
- Coherent Store+Workspace lib no-run attempt01 failed missing SpillFile FileExt delegation; attempt02 failed missing ScratchFile Debug; both exact errors retained and repaired narrowly. Attempt03 PASS12.69s, exact before/after source seal with no drift; binaries0869ef5d(workspace) and89b16632(Store). These are compilation receipts, not final-candidate passes.
- Integrated scratch attempt01:5 active checks PASS,1 ignored mechanism probe. Actual scoped data+order+seen paths peak6 reserved FD slots,24576 observed physical bytes,1097728 prospective peak bytes; exact bounded SQLite128-row proof observed16384 bytes after34095104 prospective reservation. Counters distinguish reservation from physical and pwrite calls from unavailable SQLite I/O.
- Native macOS unlinked SQLite mechanism probe remains FAIL(IOERR_VNODE6922), not acceptance evidence. `otool -L` establishes this binary links /usr/lib/libsqlite3.dylib(version358); the separately inspected Cargo-pinned amalgamation was not the linked native backend. Named0700/private0600 identity-owned scratch is used for SQLite; data/order/journals remain descriptor-owned after unlink. The ignored test records this exhausted mechanism and is not a product gate waiver.
- Capacity owner attempt01 FAIL at `real spill must be accounted`: fixture's uniform2MiB input deduplicates repeated CDC identities below the1MiB private buffer threshold. Original FAIL retained; next targeted change uses deterministic varied bytes to require actual spill without weakening ownership checks. No product fix inferred from that fixture failure.
- Parent coordinator/runtime direct checks passed on attempt03 binary; sibling HostSdk checks passed. Their receipts own those claims. Mounted correctness runner currently holds measurementlock: no builds or resource tests until root release.

Remaining capacity obligations: derive actual simultaneous canonical working-memory envelope (96MiB draft is overly conservative and effectively one builder under128MiB aggregate); retain configured cache/index limits below released maxima with no encoding/input policy changes; route admission-owned seen scratch through scope; confirm Store-global caches separately accounted; root must account ordinary Host SnapshotReader8MiB cache. Cleanup queue remains bounded and retains failed unlink charges, but runtime/End integration must drive its retries. Million-operation capacity remains unproved.

Capacity owner attempt02 PASS1/1(0.13s), binary28cdea82/source phase3-host-operations/sdk-shutdown-compile-attempt01-source.json. Fixed xorshift7 varied2MiB input produces real spill; BuiltRoot and WorkspaceAdmission retain aggregate reservations until both drop, then all three memory/disk/FD reservations return exactly to baseline. This verifies the current96MiB draft ownership path; cross-Workspace profile/caches and admission scratch remain open. No unaffected provider tests repeated for test-only fixture change.


Owner-directed stopping boundary relayed by root (2026-09-14): finish only mounted shutdown fix proof, actual spill fixture focused proof, and maintenance oracle focused proof, then publish through required workflow to remote main with successor handoff. This subagent's bounded actual-spill fix is complete; no new44/48MiB profile, admission scope or Prepared.scratch implementation was started. Current source remains96MiB/4MiB private-index draft as compiled in the passing attempt02 fixture. `capacity-stopping-source.json` seals our current owned/shared file bytes; root owns publication and broader tests.

Pending capacity work for the successor, in executable dependency order:

1. Current `CandidateCapacity::MEMORY` is96MiB under aggregate128MiB; this permits only one default construction and is not a proven minimal simultaneous-owner envelope. Do not call this compliant cross-Workspace concurrency. Read-only audit found fresh construction has one consumer buffer with <=8 worker threads, while any predecessor plan sets `has_predecessor` and caps all workers to1, so only that latter path has a second nested ObjectBuffer. Namespace frontier construction begins after all content workers join. Full frontier, producer stacks and admission-local caches must not be summed as if always simultaneous.
2. A proposed (UNIMPLEMENTED, UNVERIFIED) profile uses1MiB private indexes/64KiB order and512KiB private SQLite cache, preserving released default64MiB indexes/4MiB caches. Current4MiB scoped seen allowance reserves a4MiB cache and therefore spills on its first ID. Candidate44MiB hypothesis derives fresh path10MiB private consumer +8MiB comparison reader +8MiB existing canonical data/physical working set +8x2MiB worker stacks +2MiB journals/cursors. Predecessor path substitutes two10MiB private buffers and one2MiB worker stack, totaling40MiB. Namespace default is below44MiB; configured larger `max_final_delta_memory_bytes` must remain intact, using a phase maximum such as max(content-envelope,28MiB+configured-final-delta), after validating every term. These are a reviewed direction, not a proven numerical gate or acceptance result. Verify actual existing worker stack runtime settings and per-buffer transition/cursor bounds before coding/reserving. The initial48MiB sketch was refined; neither number landed in source.
3. Attach the existing optional ScratchBudget to `WorkspaceAdmission.admission.seen` before its first row. Current `retain_private_owner` keeps the aggregate guard alive, but seen's actual SQLite spill remains unscoped. Audit Store-global metadata::ValueIndex separately: it is shared Store derived cache/index, not an attempt-owned scratch file. Do not fabricate whole-Workspace or process memory accounting by treating StoreDb32MiB cache as included in the host overlay32MiB allowance.
4. Move scoped canonical scratch placement from the generic temp directory to the owning Workspace runtime/spool directory through a small explicit ScratchBudget directory parameter; retain native default behavior for existing callers. The current charged names are0600 and identity-owned, but path policy integration remains pending.
5. Root approved adding `scratch: Arc<ScratchBudget>` to PreparedSnapshotCommit after the next code boundary, but it was NOT implemented. Existing diagnostics.scratch_peak_* are sampled at construction end, before admission. Add separate full-attempt peak fields, sample immediately after synchronous Store admission/staging returns, then drop the scope; do not retroactively relabel old construction-only receipt fields.
6. Cleanup APIs already exist: `ScratchBudget::reclaim_pending(limit)->io::Result<usize>` attempts at mostlimit queued removals and returns remaining global queue length; errors preserve original inode/charges. `ScratchBudget::pending_cleanup_files()->usize` returns global status; `scratch.pending_own_cleanup()->usize` returns this scope's status. Root/maintenance/explicit End must drive retries and propagate unfinished cleanup truthfully. Only next CandidateCapacity::acquire currently assists8. No per-attempt unbounded queue, background thread or lost ownership is allowed.
7. Add actual two-independent-Workspace construction/retained-output coverage under the unchanged128MiB aggregate, with ordinary foreground work completing. Run only affected provider/scoped-cache/admission/candidate checks; retain prior unaffected algorithm evidence. The varied2MiB actual-spill owner check now passes; repeat it only because its real profile/scratch/admission dependencies change. No full benchmark yet: final-candidate capacity, million-operation proof, full supported V1 path, resource cleanup and other parent obligations remain open.

Source is compile-ready at the last successful native test binary. Rustfmt has not been applied to all accumulated scoped edits; root should format its exact publication candidate once the current source freeze releases, record formatting source identity, and run the required existing workflow. Store provider5 current integration checks passed; extra native unlinked SQLite probe remains ignored with original IOERR_VNODE FAIL retained, not a passing product gate. No release/tag/benchmark/issue closure is claimed by this handoff.
