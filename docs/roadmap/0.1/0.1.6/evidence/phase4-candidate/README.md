# Phase 4 candidate construction evidence

The initial prerequisite build ran 7 range tests, one metadata-only descriptor persistence test, and one captured Snapshot reader test. All 9 tests passed; the individual names and commands are appended in `verification.jsonl`. The original range algorithm and Payload failures remain in `../phase2-ranges/` and shared component receipts; these prerequisite runs do not replace them.

`prereq-01-source.json` captured source before compilation. The range Summary changed from 40 to 64 bytes to preserve zero-byte and predicted-extent limits and cached logical-inline/payload aggregates. The logical SDK-inline class is independent of physical inline/temporary backing. All four previously passing range cases exercised that changed codec and were therefore rerun with three new aggregate/no-owner-reader checks. Descriptor persistence is new coverage; the Snapshot test exercises the changed direct borrowed read path. No unchanged V3 planning tests were repeated.

Custody limitation: the Payload agent added chargeability accounting during the first prerequisite compilation, so that manifest's Payload hash is not an exact compiler-input seal. It changed SOURCE private rows from 16 to 24 bytes, added immutable ordinary-spool classification, introduced `write_inline_from`, and counted clipped last-owner releases. Ordinary class-true range and source-coordinate semantics were unchanged. Its independent new exact chargeability test passed in `../phase2-components/payload-chargeable-attempt01.log`. The nine results remain structural component evidence and are not relabeled as final-binary accounting proof. Later explicit-fsync edits likewise receive their own source/test receipts.

Actual owned-candidate integration and the existing shared-helper affected regressions follow below as append-only attempts. These checks do not claim kernel-dirty writable mmap visibility, whole-feature acceptance, or benchmark completion.

## First actual candidate checks

`candidate-attempt01.log` preserves a compile failure in a candidate-local generic error conversion. An explicit directory match repaired it. `candidate-attempt02.log` then preserves two test failures at the same fixture mistake: opening a second LayerStackStore while the original Store and Host reader remained alive returned StoreBusy under the existing exclusive-owner contract. The fixture now uses independent SnapshotReaders for intermediate checks, then drops all Store/Host owners and actually reconnects at the end. Product construction was unchanged by that fixture repair.

`candidate-attempt03.log` passed exactly two tests (0.84 seconds); `candidate-attempt03-result.json` records binary identity and actual construction counters. A held 16 MiB existing-file build permits an ordinary live write/read and another Store admission. C1 excludes the later byte, C2 includes it while live ranges still retain C0 backing, and both commits scan 1 CDC byte and reuse 16,777,215 bytes through the existing canonical engine. A new 128 KiB inode keeps its canonical identity across C1/C2 with live canonical identity still absent, submits only two later binding deltas, scans 1 CDC byte, and clears its metadata description/pending cleanup key after deletion. New-directory canonical reference count is checked as one. These are correctness/locality component measurements, not benchmark results or numerical performance gates.

One pending inode Index set shares prior unfinished work and inserts only newly captured inode keys. Description graphs retain metadata only. Construction uses the existing private output driver with Store small-file-chain settings, and obtains WorkspaceAdmission after file, namespace and correspondence completion. Aggregate scratch/file Budget integration across all candidate journals remains open for capacity acceptance; the current journals have local bounds and Index storage is under existing component caps.

## Reused-binary focused followups

The a10d8640 binary named in `candidate-followups-attempt01.json` passed the new exact explicit-zero case and three specifically affected legacy helper cases. The explicit-zero C1 construction consumes its new16MiB+2 input through the existing engine. C2 deletes A before16MiB zeros and B with zero CDC input, retaining16,777,217 bytes. C3 inserts one zero and scans exactly one byte while retaining the same16,777,217 bytes. The result and canonical content are checked after a genuine Store reconnect. Shared result-journal ordering, removed-small-file predecessor discovery and paged reference release each passed once after their helper changes.

Custody correction: the same script also reran the earlier new-inode test under the mistaken assumption that a10d included the latest deleted-directory cleanup queue code. Root corrected this after the process finished: that production change was written after a10d was compiled. The passing `candidate-deletion-attempt04.log` is retained as an unnecessary old-source rerun and is INVALID as verification of the new cleanup path. It does not close that affected check; the exact new-inode test must run on the next sealed cleanup binary. The held16MiB candidate test remains unaffected and is not repeated.

## Charged scratch provider

`scratch-attempt01.log` retains compile errors from unsigned rusqlite integer conversions. Checked signed conversions repaired them; `scratch-attempt02.log` passed three new provider checks with binary/source receipt `scratch-attempt02-result.json`. A5001-byte named file reserves/occupies8192 bytes and cloned cursors share one FD. Partial-write injection writes a real4096-byte prefix and retains12288 prospective bytes; an uncertain successful truncation retains that charge until a later authoritative truncate. SQLite128-row insertion is reserved before its transaction and settles to16384 allocated bytes; the prospective upper bound is reported separately from physical blocks. Named file ownership persists between close/reopen, and an external reservation outlives the parent through the final descriptor.

This provider is not yet wired into complete candidate output/journals and is not full capacity proof. A named-inode identity check was added after the provider run and needs its own affected verification at integration. Source changes are never relabeled as already-run binary evidence.
