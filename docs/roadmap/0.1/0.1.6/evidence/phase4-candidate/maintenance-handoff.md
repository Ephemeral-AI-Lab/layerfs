# Optional maintenance component handoff

Component status: three required focused cases now have valid passes, using two retained results and one targeted rerun. This is not a phase-completion claim. Original failed evidence remains intact.

`CommitCoordinator::maintenance_step` is implemented in `crates/layerfs-workspace/src/commit_maintenance.rs` (child of `commit_attempt.rs`). It processes one pending canonical correspondence node, retires at most 64 covered change keys (128 index rows), and invokes one existing Host maintenance/reclamation step. It never bumps the logical mutation sequence. It validates the Host Workspace identity, skips active/retained attempts with a try-lock, performs canonical/index I/O without the attempt or published-context mutex, then reacquires the idle attempt and compares the exact published Arc before updating bookkeeping. A concurrent C2 wins that comparison and preserves its pending work.

The returned `true` value means work remains **or is deferred**. It does not certify progress. Runtime scheduling must yield/reschedule instead of a busy loop while a retained or active attempt blocks maintenance. No scheduler, hidden global freeze, background thread, or public lifecycle replacement was introduced in this child component.

## Verification and retained failure

- `maintenance-attempt01.log`/`.json`: two passes, one failure on source `capacity-compile-attempt03-source.json`, binary `0869ef5dcda8f36013d124129753dd125bd1d3a25101f13d51c9477cebcfb4a7`. The passing cases cover the one-node/64-key bound, unchanged live mutation sequence, unchanged retained snapshot, and actual concurrent C2 publication with a later write and pending-context preservation. The original invocation may have overlapped an isolated v1-agent correctness process because it started before the root's ordering update. No benchmark or performance campaign was running.
- The failed test had an incorrect oracle: it expected a construction error before canonical staging to leave a retained attempt. Actual coordinator behavior correctly cleared the attempt. The test now asserts that behavior, injects the existing publication failure after staging, confirms a real `workspace_stage` and retained attempt, verifies maintenance skips, and resolves it through `abandon`. The same test verifies foreign-Host rejection.
- `maintenance-attempt02.log`/`.json`: that exact formerly failing test passes (one test, 0.07 seconds) on `../phase3-host-operations/sdk-path-compile-attempt02-source.json`, binary `cbabe2def5568d913f95c2b4f21506d490dd138f8c206894f946b8cd36971b06`. No other maintenance test was rerun for this oracle-only repair.

The source seals show unchanged correspondence, canonicalization, Index, Payload, Ranges and CommitCoordinator files between these artifacts. Production maintenance and the two passing test bodies were unchanged. The construction owner confirmed that the intervening `candidate_capacity.rs` delta changed only a cfg(test) spill fixture from uniform bytes to deterministic xorshift bytes. The root confirmed that `host_runtime.rs` changes add SDK composition, explicit after-detach shutdown, spool directory creation and tests, none exercised by the direct HostOverlay/coordinator maintenance cases. SDK/coherence shutdown changes are outside those direct cases. The Host owner confirmed that the intervening HostOverlay production delta was only a `pin_path` documentation clarification: complete canonical-path validation belongs to HostSdk. HostOverlay create/write/snapshot/maintain paths were unchanged. HostClient shutdown/root-FD release and cancel tests are not called by these maintenance cases. Therefore the two retained passes have no concrete invalidation from these deltas.

## Source custody and deferred work

`maintenance-manifest.json` records current source blobs/hashes, original and targeted commands, artifacts and reused rows. At handoff, `commit_maintenance.rs`, the earlier completed `host_canonicalize.rs`, and `overlay_cleanup.rs` are untracked product files; `correspondence.rs` has shared unstaged changes. The maintenance addition there is only `OriginSequence::workspace_id`. Its earlier `PLAN_BUFFER_BYTES` extraction supports canonical substitution; ScratchFile/journal integration belongs to the construction agent. Preserve the complete shared diff. This agent has not staged or committed any file.

On the owner's bounded-stop direction relayed by the root, no further capacity source or test was added. `overlay_payload.rs` remains blob `ec3d64744a236834891bbb0846ba56a8fe8922da`, SHA256 `bd8b2cf3e350d2ae1f0bc56a679971b18bd804b04e221ce2367dafc52030aae1`. The persisted-token versus resident-handle admission defect is **unfixed**; the planned 8,193-file reproducer is **not run**. The exact #123 contract, source finding and next executable task are in `../phase3-host-operations/payload-token-capacity-audit.md`, `.json`, and `payload-token-capacity-issue123.json`.

`../phase3-host-operations/host-runtime-consumer-audit.md` remains the non-Commit migration reference. Public Workspace/RemoteWorkspace/Workspaces integration and non-Commit consumers, generic writable-mapping V1, the complete Phase 6 capacity/correctness seal, the final included benchmark campaign and verified #124/#125 closure remain unfinished. No #122 scenario was executed by this agent.
