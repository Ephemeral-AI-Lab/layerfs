# G5-0 v5 screen-failure repair synthesis

Fresh schema and custody review classified v5 as a runner/operand binding defect. The v4 binary was healthy; its versioned schemas are part of its frozen interface. V6 names the operand schemas instead of inferring them from the runner attempt version.

Accepted: explicit v4 child schema constants, v6 wrapper/evidence schemas, fresh v6 screen/gate, retained v4 Rust static closure. Rejected: recompiling identical Rust solely to change schema strings, relabeling child payloads, reusing the v5 child as v6 evidence, or rerunning unchanged workspace suites.

