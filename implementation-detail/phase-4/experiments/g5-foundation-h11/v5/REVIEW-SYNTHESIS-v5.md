# G5-0 v4 gate-failure repair synthesis

Fresh analyzer and custody lanes confirmed the single observed path root cause and one adjacent runner disposition defect. V5 changes only versioned Python runner/analyzer files. The v4 Rust executable and all product/static evidence remain frozen operands.

Accepted: `parents[1]`, positive and synthetic-negative analyzer checks, exit code 1 as parseable REVISE, both analyzers always run when outputs are valid, fresh v5 screen/gate roots. Rejected: editing v4, importing v4 rows into v5, treating analyzer REVISE as a crash, selectively rerunning rows, or rerunning unchanged Rust workspace suites.

