# G5-2 v3 readiness plan

V3 remains `PREMEASUREMENT_REVISE`. G5-1 v27 is terminal PASS. Preparation,
freeze, forecast, screen, and gate remain closed until the exact final
250,000-byte release receipt and independently rehashed current V3 readiness
both pass.

Source SHA `4752481172724efac71e6cb69ac29f2870f84d39fa3c56facb7754b77db1047b`
and its 16/16 focused tests plus two direct reports remain preserved in
`EXACT-SOURCE-FOCUSED-FAULT-BINDING-v3.json` as a superseded focused attempt.
The accepted-only t2, immediate inode binding, and bounded token parsing repairs
are now bound to settled SHA
`47578f2685157774e23299480d2ae8bdb5a43bfa6a06ae3614f57700200109ba`,
17/17 focused tests, and two fresh direct reports in
`EXACT-SOURCE-T2-FOCUSED-FAULT-BINDING-v3.json`; that binding is superseded
history. The final mechanism source is SHA
`bcc5fd96f4c41ecc5ed1d8c1ecc89c2623ac5ef4ea9cfe3f4c451d1a2102e6d4`;
the sole release candidate is SHA
`59e766c9ee6917135941421338d8c72f8dcd97227d33b6cfb68f176f2f0d85ad`.
Every mode is exactly 250,000 bytes and every report must carry route class
`CompositePredeclaredExactCloneSparsePatchAndFullFallback`. Together with the
two predeclared campaign cases, the exact 12-case matrix classification is
complete. Preparation and freeze remain closed until the final receipt is
durably bound and current-byte readiness passes.

The prepared fixture must be compact: one parent, one latest target, bounded
edge-token/edit metadata, one SQLite store and authority sidecar, and the fixture
descriptor. It must not retain or hash one full source per requested revision;
no `Theta(J * L)` source inventory is allowed.

Every product attempt must retain lossless command, clone, stdout, stderr,
return, RSS, timing, and parsed-receipt artifacts. Both analyzers reopen and
rehash the process receipt and every nested artifact, the actual terminal file,
and the owned lock-release descriptor/inode/token receipt.

Semantic-policy latency and execution-route latency are separate observed
tables. Changing policy labels while holding routes constant must fail. Each
source proof must reopen and rehash both exact frozen source and a durable
focused execution receipt, bind its exact test/function locator and exact matrix
row, and state a typed outcome plus justified counters. Source text or a free
hash-and-claims object is not closure. The campaign separately executes the
unambiguous `clone-failure` and `after-rename-lost-ack` product selectors and
retains singular process receipts before evaluation. The other ten exact rows,
including `missing-seed`, require exact complete-apply focused authority.
