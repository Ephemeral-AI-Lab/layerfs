# G5-1 v27 review synthesis

V27 is a prospective RSS-lifetime repair, not an unchanged retry. V26 remains
preserved as a 200-row measured `REVISE`; `V26-SUPERSESSION-v27.json` binds its
screen PASS, freeze/dry/attempt history, full gate failure, retained result root,
released lock, and all six exact RSS failures. The repair makes no performance
claim until a matched patched G4 control and the v27 G5 child independently
prove that the only product difference is the matched SQLite `cache_size`
configuration change from 2,000 to 1,280 pages.

V24 remains credible warm mechanism evidence but is not promotion authority because its executed attempt-2 runner and preconditioning were not source-frozen and its G4/G5 comparison used different process shapes.

V27 keeps preconditioning prospectively frozen and charged, directly binds the
executed runner, and keeps all measured G4/G5 arms one-shot. Product format,
inputs, schedule, thresholds, integrity semantics, durability, and error gates
are unchanged. Cache configuration applies within Store operations;
preconditioning remains outside product timers and inside complete wall. All
new release-call experiments are preserved as diagnostic NO-GO history and are
absent from final authoritative product bytes.

A fresh v27 PASS is the sole G5-1 authority for G5-2. Until then G5-2 measurement and later-stage authorization remain closed.
