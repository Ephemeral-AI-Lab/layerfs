# G5-1 v24 review synthesis

V23 proved that batching 42 old mapping-leaf reads into one query saved only 0.264 ms of mapping work and made total plus1-middle approximately 0.748 ms slower. V24 deletes that complexity and restores the original serial fixed-radix construction.

The closure law measures what G5-1 actually changes: Trusted avoids Verified full-closure establishment while preserving identical canonical publication. Every operation must improve by at least 90% at paired p50, while Verified must not materially regress against frozen G4. The first-edit p95 safety ceiling and all correctness, durability, memory, Q, storage, and custody laws remain unchanged.

Only a passing protected screen authorizes one 150-second gate. G5-2 remains closed until G5-1 passes.
