# G5-1 v24 closure preregistration

V24 is the single prospective closure candidate after v23's causal REVISE. It retains exact frozen-G4 identities and canonical format. It removes v23's ineffective old-mapping-leaf batching and retains TrustedLocalDev semantics, the CompleteRoundTrip timer repair, borrowed per-object Verified scrub, exact error behavior, and post-request SQLite memory release.

Acceptance requires no material frozen-G4 to G5-Verified regression; at least 9,000 basis points paired-p50 improvement for G5-Trusted versus G5-Verified on every supported edit operation; and the existing first-edit Trusted p95 ceiling of 25 ms. No new absolute p50 ceiling is invented. The former plus1 p50 at most 15 ms value is classified as a falsified fixed-radix research aspiration, not a G5-1 completion claim.

Exact root/transition, authenticated work, Trusted/Verified provenance, one transaction and one COMMIT, durability, reconciliation, Q0, RSS at most 20,971,520 bytes per child, storage, cleanup, errors, analyzer agreement, and custody remain hard gates. The population remains 200 arms, 100 pairs, and 56 CompleteRoundTrip checkpoints. Screen is less than 20 seconds total; gate is at most 150 seconds total.

File-size-insensitive count-changing edits require a later canonical/prolly mapping structural lane. That lane changes format and authority and is outside G5-1 and G5-2; it is recorded as the later H09 target and is not claimed complete here.
