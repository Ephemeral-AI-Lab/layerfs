# G5-1 v23 review synthesis

V22 proved the full 200-arm campaign fits within 150 seconds and closes RSS/Q/lifecycle custody, but failed Verified performance and three Trusted absolute thresholds. V23 preserves that evidence and makes one bounded repair candidate.

The borrowed scrub restores the proven authenticated object/byte workload without the failed scrub batch state. The count-change repair reuses the existing ordered authenticated batch reader for mapping leaves only. It removes cold SQLite query fan-out while leaving the fixed-radix suffix rewrite, 90/49 canonical object writes, identities, construction proof, transaction, COMMIT, durability, and storage format exact.

Only a passing sub-20-second causal screen authorizes the unchanged 200-arm, 56-checkpoint, 150-second gate. G5-2 remains closed.
