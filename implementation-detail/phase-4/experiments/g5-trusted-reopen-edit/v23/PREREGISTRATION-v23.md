# G5-1 v23 preregistration

V23 is one product candidate after the terminal v22 REVISE. It changes only two mechanisms: Verified edit-base scrub returns to the frozen-G4 borrowed per-object path, and trusted/verified plus-one fixed-radix reconstruction batches only ordered old mapping-leaf reads. Raw chunks, canonical writes, receipts, publication fencing, durability, reconciliation, one transaction, and one COMMIT remain unchanged.

The population remains 200 arms, 100 matched pairs, and 56 CompleteRoundTrip checkpoints. The mechanism screen remains less than 20 seconds total. The measured campaign remains at most 150 seconds total. No v22 row is reused.

The causal equations are prospective hard gates: plus1-early batches 83 old mapping leaves in exactly 2 queries while retaining 90 created objects and 196,375 mapping bytes; plus1-middle batches 42 old mapping leaves in exactly 1 query while retaining 49 created objects and 100,763 mapping bytes. Ordered cardinality, identity authentication, exact leaf role/length, canonical root/transition, proof consumption, Q0, storage, cleanup, RSS at most 20,971,520 bytes, and one successful COMMIT remain mandatory.

The screen must show Trusted p50 at most 15 ms for first-edit, plus1-early, and plus1-middle, and must restore Verified authority to the frozen-G4 145–155 ms neighborhood without material regression. A miss stops before the 150-second gate and is diagnosed only from the retained mapping/COMMIT split.
