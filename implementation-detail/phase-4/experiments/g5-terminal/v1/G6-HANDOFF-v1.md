# G6 eligibility handoff

Disposition: **G5 PASS; G6 eligible; G6 not started**.

G6 may treat these immutable milestone authorities as prerequisites rather
than rerunning them:

- G5-0 v9: whole-harness history/reachability/Q authority;
- G5-1 v27: Verified-default and TrustedLocalDev publication authority;
- G5-2 v3: bounded warm exact/latest native-projection authority at 250,000
  bytes;
- G5-3 v3: 1,000-revision history, A→B→A, exact historical read, and bounded
  10 MiB two-reader/one-writer closure.

The next lane owns the explicitly deferred work: file-size-insensitive
count-changing representation, controlled-cold and larger-file projection,
production schema/API/VFS/SDK extraction, persistent/multi-file projection,
adversarial filesystem hardening, rollback freshness, and destructive GC if
those become requirements. This file selects no algorithm and authorizes no
G6 measurement by itself.
