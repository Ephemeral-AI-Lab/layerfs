# Unrequested SQL history: performance invalidation and canonical-input reuse

Decision: all191 selected successful performance observations collected with unbounded SQL history are invalid performance evidence. Their actual raw product pass/fail statuses, timings, resource values, sources and sealed artifacts remain unchanged as diagnostics. All older recordings with the same feature-enabled unbounded history receive the same generic diagnostic-only label. No source-equivalence or overhead label can admit these timings. The explicit191-slot inventory is selected-performance.json; matching entries are appended to the existing invalidations.jsonl before recollection.

The governing frozen requirements are execution-contract.md14–16 (passive counters and verifier-only seams),77–79 (monitors excluded from operation-local timing),96–103 (incremental passive receipts and separate census/verification); phase-1-handoff.md38–43 (repair harness defects; instrumentation preserves measured behavior),118–121 and147–149 (recollect affected invalid evidence); testing-rules.md269–280 (pure declared workload/timing) and301–304 (relevant changes require appropriate evidence); and docs/general/benchmark_rules.md167–180 (receipt/monitor work outside timers unless expressly named) and707 (contaminated timer is invalid evidence). Execution-contract.md25–26 permits an explicitly instrumented baseline, but does not authorize retrospective addition of an unbounded SQL statement recorder to the frozen passive observation profile. The completion amendment23–35 preserves old artifacts and permits reuse only of unaffected evidence.

The enabled SQL_TRACE recorded each statement as an owned String without a bound or opt-in. It ran inside the approved public calls, retained workload-proportional process memory, and was not requested or used by performance. Every selected family traverses traced Store operations, including read-only and clean lifecycle rows; no selected complete timing row has evidence of an unaffected no-SQL path. Removing the recorder changes their timing/resource measurement conditions. Individual numbers cannot be corrected by subtracting estimated allocations or a constant duration. Recollect exactly those191 formerly selected passes on the corrected capture-default source; collect still-unexecuted slots once. Preserve actual failed outcomes and repair their current required gates rather than converting historical failures to passes.

The two independent proofs are different: payload-create-1m seed1 fbf09ef8212a24f and sustained600s e78401219f621176 completed in separate verification mode, with authentic independent output checks and actual required resource/deadline gates. Recorded sampled RSS was16,203,776 and93,519,872 bytes respectively. Their original proof identities remain fixed and explicitly state-compatible with the reviewed repairs. They support no old performance timing and infer no current memory cost. No future old-source verification execution is selected; all new work uses the clean default runtime.

Canonical fixture/reference custody is not a timing claim. The minimal runner bridge compares every preparation dependency and full helper file; only two exact reviewed file pairs may differ: schema.rs capture default (342/a40→new), and objects.rs derived spill index (342→new only). It preserves original producer source, build, image, prepared key and authenticated master bytes. The existing full registry, per-case fixture/reference info and immutable Git system checks remain. Namespace500 explicitly rejects the old342 producer and reuses qualified a40. No digest or cache key is rewritten, no unknown source pair is accepted, and no performance equivalence follows from input compatibility. The source/model qualification sql-capture-preparation-bridge passed once in0.831402500s against8278d817.

The active map is simplified to exactly two completed-proof selectors; no old performance selector survives. It targets clean assets6c54f8d7, and only one reverse verifier-state join permits the freshly recollected payload-create-1m seed1 timing to use its exact retained proof. Generic source-state bridges cannot override the new capture-purity check. Existing old maps/reports remain historical documents; the previous active map is preserved in previous-source-map.json. Full report regeneration waits for the next useful checkpoint.
