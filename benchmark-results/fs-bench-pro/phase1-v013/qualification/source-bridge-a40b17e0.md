# Derived spill-index repair: source compatibility assessment

The a40b17e05486e5b747b689e7710475d739556a69 build is the new default. The scoped map retains 141 earlier performance slots and two already-passing required proofs under their actual producing identities: 96 legacy ordinary performance slots, 45 e784 performance slots (24 tiny deletion, 12 Git, nine namespace), the fbf payload proof and the e784 sustained proof. The old namespace500 preparation failure and interrupted later seed remain preserved and excluded. The previous map is retained in source-bridge-before-a40b17e0.json.

The report bridge pins objects.rs SHA256 `1e88000d97560d5d9d8afdaaf379144cfd859133897650f357c8299a19b3aa32` to `4b07eb03a2e6ddfe926a2c5fa621db462c659ff1ee164e41ec3b90cb871df9c8`, requires every other product/Cargo/config tree entry to match e784, and binds unchanged fixture/workload/oracle dependencies for all six relevant families. Legacy fbf/b8 sources additionally compose through the existing strict unlink and empty-generation predicate proof. The eligible cases are explicitly enumerated; namespace500 is not included in the old-source scope.

The new SQLite index is derived scratch state containing object ID, payload offset and canonical length. The overflow-triggering row is flushed before the one-time backfill; subsequent insertions add the same offsets while pending-byte lookup retains precedence. The outer canonical collision check, canonical object encoding, reachable-object ordering, persistent schema and Store admission routes are unchanged. The index does not generate or replace canonical payload bytes. Existing corrected qualification `spill-disk-index/corrected` binds the same new source hash and tests transition, exact byte and length lookup, duplicate/collision behavior, unchanged64MiB threshold, bounded SQLite cache settings, scan avoidance and cleanup. The coordinator reports the corrected actual namespace500 seed1 integration passed (preparation127.031s, runtime50.849s; selected issues/violations empty); its independent verification remains a later required gate.

Compatibility is not binary timing equivalence. SpillObjects gains an optional boxed index pointer, and shared spill get/length paths change even below the overflow threshold. Thus old timings and resource peaks measure only the original binaries. New temporary-index, allocation, locking and instruction costs remain unmeasured by retained observations. Reports retain original per-source distributions; they must not describe these141 rows as executions of the a40 binary. Persistent-format/semantic compatibility does not waive current correctness, custody, resource or observation gates.

Preparation reuse needs separate producer custody. The preparation compatibility digest hashes all Store source, so the objects.rs change causes existing --preparation-assets selection to reject older producers before plan comparison. No runner or cache-key change is made here. Non-Git plans are hashes of independent Entries and remain the same across this repair, so separately qualified new producer inputs can still pair with old performance. Git Store and reference plans bind the actual producer image; new default-image Git proofs cannot match the old image-bound performance inputs. The minimum no-runner-change route is matching Git proofs on their original sealed runtime/producer, preserving actual source and qualified cache provenance. Alternatively, a separately authorized exact initialization-equivalence exception could allow new runtime to clone the authentic old producer inputs. Neither route may rewrite producer identity or relax exact proof input matching.

Map SHA256: `68c6745182d0bdcb3a19312a57f413c1a5ab1a907dd26523efc30959abd498bf`;32 selectors, three product bridges, seven verifier bridges. Exact source proofs were computed from Git bytes. The focused report model and full report regeneration are deliberately deferred to the coordinator's next useful checkpoint while performance collection continues. No product test, build, benchmark, cache mutation or input preparation was performed for this assessment. This is not a terminal-pass claim.
