# Rebase node-lifetime repair: exact source and state compatibility

Default assets are d6fdf964464ecb6f4a1188c69ee4bbd2e06c3f9c, product sealdd0b2e19da1b391a1279187355f5c2d299e55b7c43a0d637a6ba3961f5113049. The map retains190 successful prior performance observations under their original producing identities:186 selected before d132, three d132 dense100 passes and only d132 dense500 seed1. The dense500 seed2 RSS failure remains sealed historical failure evidence, including its corrected watchdog event classification by the report validator. Dense500 seed2/3 performance uses the new default.

The existing exact-source bridge kind is extended with one optional rebase lifetime pair; no general compatibility framework or cost waiver is added. It pins lifecycle.rs SHA256aa14df244edb8aed1c4cb5cff94ed1fa530cf2610785d89e559c2a67735292f8 to91e65bf1ab13f452a15dab7e6934e1256a39f508a39efbc0b2a00c718ad0c2ac and requires every other d132 product/Cargo/config input to match. Earlier planner/index/predicate source chains remain unchanged nested proofs. Fixed fixture/workload/independent oracle sources are also bound.

The old rebase cloned the full original node graph, retained all fresh resolved nodes in its temporary Workspace, and cloned each fresh node again into the final map. The repaired loop borrows the original nodes without modifying them; it clones only pinned unlinked nodes and the final retained paths/spool handles. Fresh non-directory nodes move out of the temporary map after all aliases and attributes are checked; their temporary canonical-cache entry is removed. Ancestor directories remain available for later resolution. Final live nodes still use the same original NodeIds, paths, pins, canonical identities, alias counts, bytes, mode and nanosecond mtime. Retained orphan spool accounting, final state assignments and post-publication root/head checks are unchanged. The original Workspace survives until all fallible validation completes. Persistent canonical output is already published before this rebase and its format is unchanged.

The already-executed product qualification rebase-node-retention tests stable identity, aliases, bytes, metadata, edited-to-base transitions, pinned-unlinked spool retention/readback and final unpin cleanup; its source hash matches the pinned new source. That state-equivalence evidence permits earlier successful proofs to retain their actual source identity. It does not measure new memory allocation, lookup/cache work or instruction cost: old resource peaks and timings remain original-source observations.

All dense500 verification seeds MUST select the repaired current product, including seed1 whose old timing is retained. The report enforces this at scoped-build selection, and the map leaves these proofs on d6/preparation a40. It also rejects reusing the old dense500 seed2 observation via the direct rebase bridge; the only eligible older dense500 slot is seed1 without a watchdog failure. The two previously completed required proofs (fbf payload and e784 sustained) remain selected; neither is rerun.

Other proof producers remain: earlier payload/tiny/directory/Git and namespace1/10/100 use e784/preparation342; namespace500, locality and remaining new families use d6/preparation a40. Preparation inputs are unchanged by lifecycle.rs, and the base preparation compatibility SHA remains5ae52b795495a6f74d21010e6783e1874986f41a6559bdab0f2017e71dbce315. No cache-key changes, producer relabelling or fixture regeneration is introduced. Since the sealed e784 runner predates the fail-fast repair, execute its proofs with individual --case/--seed invocations and stop on the first failure, using its immutable source-worktree runner and absolute asset/cache/output paths.

Map SHA256461543a5144822ad47b14887d3d2c20e4418c0c1a88d5dcf16726dfabe89c5df;56 selectors, five product source chains and seven verifier joins. The previous map remains source-bridge-before-d6fdf964.json. The minimal checkpoint model is prepared at report-rebase-lifetime-bridge/check.py and has not been run during integration. It checks the new exact source pair, preservation of earlier proofs,190 old selected timings, repaired dense500 verification selectors and rejection of old seed2. No product/build/benchmark/full-report execution was performed for this source update; no terminal pass is claimed.
