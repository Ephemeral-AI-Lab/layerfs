# Source-bound predicate bridge to e7840da1

The default runtime assets are `assets-e7840da1`, sealed revision `e7840da1da81404ff228be734a91783cebb946ca`, product seal `733b90b4dcf879abefed090cdfd2802d05d925d2f8805e4352bc294b4110a1c6`. The explicit map retains 96 original-source performance slots (95 from fbf32e84 and the atomic-sampler replacement of tiny-stat-1 seed1 from b8c2ad4b), and only the original fbf32e84 payload-create-1m seed1 independent proof. All other slots use the new default. The old tiny-stat incomplete observation, functional failures, deleted-file/Git observations and sustained failures remain preserved and are not admitted by this bridge.

This is evidence of workload/state compatibility with explicit producing-source distributions. It is not evidence that old timings measured the new binary. In particular, 36 file-creation slots may execute the newly added pure length predicate even when it is false; its instruction cost is unmeasured by retained timings. The remaining 48 read/stat/traversal slots and 12 directory-only construction slots never call the changed installer. Report rows carry `product_predicate_scope` and `measured_current_product_binary`; distributions keep full actual source, product, harness, image, contract and environment identities.

The product proof permits exactly two runtime changes: the reviewed `ProxyClient::unlink` body, and the exact two-part `Workspace::install_edit` edit adding `old.len() != 0 && next.len() == 0` and resetting the edit counter only when true. Removing that literal prefix and restoring the original assignment must reproduce the old entire file byte-for-byte. The proxy code outside its exact method and cfg(test) module must match. The separately hashed `tests/file_edit.rs` integration test is outside the release library target; unchanged Cargo declarations bind the library to `src/lib.rs`. All other product, layout, limit and Cargo/config Git-tree bytes must match. Each proof pins the ordinary fixture, operation and independent oracle sources; only the exact previously reviewed resource-sampler body may differ.

Every retained raw attempt must contain complete zero unlink/rmdir callback counts, one successful identity-matched ordinary workload receipt, no owner-side range edit, and explicit zero ftruncate, unlink, rmdir, rename, editor-save, in-place-edit and Git-process counts. Read-only and directory construction additionally require zero pwrite and file-write counts/bytes. File creation requires the exact frozen file count. The pinned source uses exclusive `create_new(true)` and positive monotone writes into initially absent files; therefore no nonempty file can become empty. Zero-length newly created files never have a nonempty prior state. The old payload proof follows the same creation then read-only verification path and remains labelled with its actual producing source.

The one focused product-free qualification is retained in [report-empty-generation-bridge](report-empty-generation-bridge/): PASS in 1.019604625 seconds. It checks actual fbf/b8-to-101626e7 sources, rejects the old unlink-only bridge after the additional installer edit, rejects two unreviewed predicate/assignment mutations, checks all eight retained operation kinds against representative sealed raw receipts, rejects an injected truncation count and Git scope, and accepts the original payload proof's observed predicate. The final successor map recomputes the exact source proofs against e7840da1 and validates all four verifier source-path bindings; no product, preparation, benchmark, full report or prior passing qualification was rerun.

Map SHA256: `63cbb92438cff13d7d34c916c4c774041fc6af02ef75494b16d852dace508995`. Previous map bytes are retained as [source-bridge-before-e7840da1.json](source-bridge-before-e7840da1.json). The map contains 16 selectors, two exact product predicate bridges and four fixed-workload/oracle verifier bridges. The coordinator's full report validation remains responsible for applying every existing custody, correctness, resource and terminal gate to every selected raw row. This note does not claim campaign completion or terminal pass.
