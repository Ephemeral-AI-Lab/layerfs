# Separate fast profile runner/report qualification

Status: source ready; the coordinator schedules the single aggregate check and one representative integration after sealing. No product or model execution by this worker.

The runner requires an admitted, hash-sealed full read-only pristine-input proof, exact seed/product/input-plan identity, and unchanged independent fixture/oracle source definitions. It refuses missing/incompatible certificates before preparation and never falls back to full verification. The JSON certificate, exact TSV projection, both compressed artifact hashes and original full attempt seal are retained and bound. The host authenticates the projection and artifacts before use.

Fast results have separate mode keys, assurance labels and receipts. They contribute zero full Phase1 verification slots and zero performance distributions. The existing performance-to-full-proof same-environment filter remains mandatory; Docker4 performance and Docker8 full proof results stay distinct. Interrupted invocation accounting preserves KeyboardInterrupt status and the actual partial batch wall; missing future slots remain missing.

Prepared342/a40 masters retain their original source/key/seals. The exact reviewed SQL-capture/spill preparation pairs remain required. The five pinned fast verifier files qualify only this source change; host fixture_info/output_text span, common independent recipe prefix and workload SHA generator suffix must remain identical. No fixture regeneration or old source relabeling.

Pending one small Python entrypoint: import generate-workspace-report.py and call fast_profile_self_check(). It checks missing full certificates, copied-artifact changes, projection changes, disjoint fast/full slot identity and rejection of placeholder full completion. Combine with the already prepared host/native aggregate, not a broad suite.

After the sealed build, preserve373 clean6c performance selectors and39 exact completed full-proof selectors; update source bridges with exact source hashes. New fast evidence is separate from the343 remaining full slots.
