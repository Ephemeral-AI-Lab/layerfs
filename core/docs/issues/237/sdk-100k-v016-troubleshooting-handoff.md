# Handoff prompt: explain the 100k Init time gap

You are the next #237 diagnostic agent. **Your first measured operation is v0.1.6 `--release` on the exact 100,000-file source already used by the Core SDK release diagnostic. Do not rerun Core release.** The owner explicitly requires release binaries for this comparison; that direction authorized the separate #237 Core release diagnostic while the frozen #236 selection remains debug-only. Read root `AGENTS.md`, `benchmark/AGENTS.md`, `benchmark/fs-bench-pro/QUICKSTART.md`, `docs/general/benchmark_rules.md`, `core/AGENTS.md`, and `core/benchmark/fs-bench-pro/AGENTS.md` before running anything. Preserve other agents' changes, existing receipts and the frozen #236 two-case selection. Do not merge to `main`.

## Why this is needed

The retained [Core SDK release receipt](evidence/sdk-100k-release-20260924/receipt.json) at source `62e7204cab99540a29f68826c2e142a08682eec8` reports one **5.077702667-s** public `Client::init_project` call, **6.408342167-s** complete command and a **12.603483166-s full reopened PASS** on the 100,000-file/500-MB SHAKE input. Source payload residency was 0/126,206 pages. The [release report](sdk-100k-release-result-20260924.md) and raw path `benchmark-results/fs-bench-pro/sdk-100k-release-diagnostic-20260924-01/` carry identities and limits; the verifier missed the general under-10-s expectation. The earlier [23.972432750-s debug receipt](evidence/sdk-100k-debug-20260924/receipt.json) is historical context, not the Core arm of this release-to-release comparison. Neither SDK receipt has internal stage times because `Host::create` disables the recorder.

The retained v0.1.6 [#152 row](../../../../docs/roadmap/0.1/0.1.6/evidence/issue152-final-report.md#3-family--per-test) is **4.986155625 s** cold on its own 100k/500-MB reference fixture, built `--release`; its **4.397542208-s comparator is v0.1.5**. The 2.7-s absolute cold target failed and was owner-waived. Locate any alleged v0.1.6 3–4-s receipt and report its exact fixture, cache, profile and status; write `NOT_FOUND` if there is no qualifying receipt. Do not treat the 23.972/4.986 = **4.81×** raw quotient as a regression factor: build profile, fixture bytes/per-path sizes, API route and metadata-cache qualification differ. The [10k SDK profile observation](sdk-merge-check-20260924.md#follow-up-release-profile-explains-the-apparent-10k-slowdown) (6.140 s debug, 1.289 s release) makes profile a plausible lead, not a 100k explanation.

## First: one exact-source v0.1.6 reference attempt

1. In an isolated checkout, pin the **unmodified peeled v0.1.6 product** commit `44cf748486863ab7c21ca47e731bd88e2b9a7b4a` and its `crates/*` tree. Use a worktree-local Cargo target, `--locked`, no third-party patch and no product-source optimization. Reuse the existing reference benchmark's native directory Init and bounded diagnostic counters; the [prior 10k common-source arm](v016-head2head-arm.md) describes the benchmark-only READY/GO cold barrier and authentic `Client::initialize_layerstack(..., LayerStackInitialization::Directory(path))` timer. Its old temporary wrapper path may no longer exist: inspect the archived source/commit before relying on it, then add only the minimum benchmark-only wrapper needed. Do not turn fixture import into direct Store mutation.
2. Freeze and commit the **v0.1.6 exact-source unregistered release contract and result schema before its first run**. Name the existing Core SDK release receipt as the retained comparison context, with its source/build/fixture/cache boundaries; it must **not** be sampled again. Fix one reference sample, exact public timer, independent source copy, cache method, watchdogs, complete-command and verifier budgets, receipt fields, and failure/timeout status. Build with locked Cargo `--release` and pin the actual `target/release` executable hash; do not use a debug executable with a release label. Keep #236 and both earlier Core receipts append-only and separate. Declare any diagnostic safety watchdog separately from the 15-s complete-command and under-10-s verifier budgets and never promote a budget miss to PASS.
3. Use the sealed Core SDK seed-1 SHAKE fixture profile `core-sdk-init-fixture-v2`, case `namespace-100000`: **100,000 files, 1,000 data directories plus root, 500,000,000 total bytes including two 100,000,000-byte anchors, class counts `(2, 1000, 78998, 15000, 5000)`**, modes/mtime and path-to-size assignments from `core/benchmark/fs-bench-pro/families/init_namespace.py`. Manifest SHA-256 must be exactly `23246a276522418812df2392619c13391bc864835d09a6695b6bd6fc0307d7a5`. The prepared master is currently under this task's worktree at `benchmark-results/fs-bench-pro/sdk-prepared/namespace-100000-b69e710dfd0474a5/`; if unavailable, regenerate **that same deterministic profile** in untimed setup and verify the manifest digest. Never silently use the reference runner's different generated fixture just because it shares the case ID and total bytes.
4. For the new **v0.1.6 release arm**, make a fresh independent writable byte copy from the sealed master, validate all paths/metadata/bytes/SHA-256, then invalidate payload pages and require a nonfaulting whole-input **zero-resident** check immediately before the public timer. Record page size/count, method, check-to-call gap and process disk reads. The reference's `shared/cold.py` is a starting backend. Preserve the old 100k row and its original cache labels; this new row has its own identity. Do not use a previously timed source copy, warm the payload, or run `sudo purge`.
5. Run the **v0.1.6 release reference once first** through the public directory-Init API, with fresh Store and result path. Capture the typed root/result, complete command, public timer, lifecycle CPU/peak RSS, source reads, existing reference Init diagnostics (`prepare_import`, pipeline, producer/consumer, final namespace, publication), exact SQLite statement/COMMIT counts and byte geometry. Retain every timeout, unknown or failed row. Do not repeat this arm to select a faster result.
6. Run one separate **full reopened 100k/500-MB readback** for the confirmed v0.1.6 root, with exact manifest path/content/metadata checks. Keep verifier time outside both operation and command times. Check 4,096-byte SQLite pages, apparent and allocated Store size, `object_packs` row count and BLOB bytes. If it fails or misses its frozen bound, preserve that fact. The previous 10k common-source verifier initially failed on a wrapper-added newline in an ID; validate the ID protocol with a non-timed small check before this expensive call, not by retrying a 100k public arm. Reuse the existing Core release verifier receipt as evidence; do not repeat it.

## Then: side-by-side attribution, not another speed sample

Start with the retained Core SDK release receipt and the new v0.1.6 release receipt. The Core release call has **no per-stage timings** because SDK `Host::create` disables the recorder; mark those cells `NOT_MEASURED` initially. To fill them, use one **labelled, count-driven Core release diagnostic at a new instrumented identity** in a separate research worktree: keep the same public SDK call, exact fixture and input checks, four Init producers, one C2/SQLite owner, 128-KiB cutoff, 4-KiB pages and SQLite BLOB packs. Add bounded aggregate `Instant` spans/counters only in research source, archive the exact diff and release binary hashes, and run one instrumented call with a new source copy. Do not relabel it as the uninstrumented 5.078-s release sample or the historical 23.972-s debug sample, and do not present its wall change as a speedup. Prefer existing `TimingScope` boundaries in `core/crates/layerfs-service/src/save/import/scan.rs` and `namespace.rs`; explicitly prove which stage data are emitted. Do not add per-file logs or test-only hooks to production.

Produce a table with **v0.1.6 release exact-source**, **retained Core SDK release exact-source**, **Core release instrumented diagnostic**, and **historical Core debug context** columns. For every row, name its timer start/end, profile, overlap, unit and source receipt:

| Stage | Required measurements or `NOT_MEASURED` |
| --- | --- |
| Setup and outer boundary | Host/Store/client setup, source preflight, public call, return/delivery, teardown and complete command separately. |
| Source traversal | Directory enumeration/sort, metadata calls, opens, read calls/bytes, device-read bytes and elapsed scan. |
| File construction | Four producer wall/max/CPU where attributable, content encode/CDC/hash counts and bytes, source-read wall, queue send count and blocked-send sum. |
| Handoff/C2 | Slab/batch/event counts, consumer wait, owner `SaveHandoff::accept` wall, new/reused objects, pack BLOB create/append/rewrite bytes, SQL statement types, BEGIN/COMMIT count and wall. |
| Namespace/C5 | Prerequisite/metadata Save, C1 directory/root construction, tree Save, catalog publication and response serialization. Show route differences rather than forcing false one-to-one names. |
| Resources/output | Lifecycle CPU, scoped RSS, source residency, Store/History apparent and allocated bytes, page size, pack rows/BLOB bytes and full reopened readback. |

Use a disjoint top-level stage ledger that reconciles to each public call's wall. Show nested and parallel worker spans separately; **never add producer wall sums, blocked-send sums or nested SQL timers to parent wall**. Compute count × per-call cost for the dominant extra work and identify the smallest causal bottleneck that can explain the observed release-to-release gap. Report unmeasured time as a residual, not zero. The v0.1.6 and Core public APIs/canonical formats still differ; name those boundaries rather than forcing stage equality. The old 10k debug/release observation is supporting context only. No throughput ratio is release admission without matched route/cache and a new prospective gate.

## Deliverable and stop rule

Commit a concise evidence report and all small receipts/scripts under `core/docs/issues/237/`, with full append-only raw files in a fresh worktree-local `benchmark-results/` path. Give exact identities, one-sample counts, numbers, limits, arithmetic, all failures/`NOT_RUN`, a source-linked phase table, the leading diagnosed cause and the next smallest proposed experiment. Update GitHub issue #237 with those results. Keep historical receipts and the #236 selector intact. No new Core performance sample at the frozen 100k identity, no repeat of an unchanged reference arm, no treatment/worker/page-size/timeout change to make a number pass, and no merge or production optimization until the diagnosis is reviewable. Include the required per-commit production LOC comparison (reference/core/combined) even for docs-only commits.
