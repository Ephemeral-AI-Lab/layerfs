# #241 post-fix baseline control v1

> **Status:** Dated planning checkpoint; not release evidence or a product
> contract. Frozen before the first attempt on 2026-09-25.

This is one public SDK integration smoke at the source containing the
composed Service acceptor stdin-poll fix and daemon upstream-stage telemetry.
From an independent validated byte copy of the qualified pristine 10 MiB v3
master, a fresh Branch and Sandbox mount a Workspace. The sole Exec command
is exactly `printf baseline > .position-baseline`. Record its typed result
and host context wall, one unmount, SDK Sandbox deletion and final absence.
There is no range EDIT, Commit, retry or registered performance sample.

Freeze the clean source commit/tree, host test binary SHA-256, immutable new
Linux image ID, master receipt and clone hashes, command, telemetry run ID,
and fresh output path before the attempt. The first attempt is retained
whether it succeeds or fails. Cache is uncontrolled and
`admission_eligible=false`; success checks wiring only. It cannot replace
the failed historical position or prove the host CPU spin caused either
earlier `Unknown` outcome.
