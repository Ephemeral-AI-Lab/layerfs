# #241 baseline Exec after complete missing-Inspect session retention

> **Status:** Dated planning checkpoint; not release evidence or a product contract.

The sole [frozen plan](plan.json) ran at source `fdcdba0f366da6f80a13bde4252e38cb9d83e2df` (product correction `0da912016`) with immutable daemon image `sha256:a5dfe07eb7c67d838bec3e1723e09c2ce20fd3fae862aa0e04159d0f4aaae680`. It used an independently validated writable copy of the closed 1 MiB master and public SDK Project, Branch, Sandbox and Workspace lifecycle. The exact source, binary, image, master, copy, telemetry and output identities are in the plan. The [attempt](attempt.json), [raw receipt and logs](raw/) and [SHA inventory](RAW-SHA256.txt) are retained.

The single `printf baseline > .position-baseline` Exec returned exit 0 in 18 ms of host context wall. Docker reported a running, non-OOM daemon. Unmount, Sandbox deletion and final absence passed. The daemon LFT1 contains two missing-path Inspect refusals followed by `HistoryCommand::ReserveInodes`; all three have only `daemon.service_call`, with **no fresh TCP, Noise or Hello span**. The host acceptor shutdown summary records **1 accepted, 1 admitted, 1 reaped, peak live 1, zero drops, capacity 4, no terminal error**. The declared one-shot route criterion is **PASS** with complete observation coverage.

This proves that the specific reconnect immediately before baseline inode reservation in frozen v3 has been removed for this source and route. The frozen v3 receipt remains **229 PASS / 1 FAIL / 34 NOT_RUN**; it is not promoted or replayed. The original five-second TCP-versus-Noise cause cannot be recovered from v3, and this one successful diagnostic is not a general network reliability claim. Cache was uncontrolled, so there is no latency admission. The 264-position functional gate and four separate release Edit→Commit cases remain open; this task did not rerun their passing slices.

At this product source, `cargo +1.85.1 test --manifest-path core/Cargo.toml --locked`, `cargo +1.85.1 clippy --manifest-path core/Cargo.toml --all-targets --locked -- -D warnings`, `cargo +1.85.1 fmt --manifest-path core/Cargo.toml --all -- --check`, `cargo +1.85.1 check --manifest-path core/Cargo.toml --locked --examples`, `python3 core/tools/check_product_boundary.py`, and `python3 -m unittest discover -s core/tools -p 'test_*.py'` all passed. `git diff --check` passed. These checks cover Core source and tooling, not the unrun position or release admission gates.
