These are exact external command/controller sources used by the review receipts. Each runner executes one supplied command; this is not an aggregate gate. Reproduce from the implementation worktree, restore the helper path named by the receipt, and select fresh evidence/identity paths before another attempt. The image helpers use only owned temporary build contexts and uniquely named containers. Test-only images are separate from the product daemon image.
