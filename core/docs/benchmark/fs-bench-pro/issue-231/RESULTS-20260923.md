# #235 substrate and #231 native Init first pass, 2026-09-23

Status: **incomplete**. The default 128-entry bootstrap test ceiling and the
Service's missing C1 ordering backing are removed. The real public native
directory import and minimal daemon-host runner exist. Two of the three
selected Init cases returned confirmed roots and passed full independent
verification; all three remain admission-ineligible because source cache state
was uncontrolled. The 10,000-file case did not return a confirmed root under
the frozen command budget. The 100,000-file case remains `NOT_RUN`. Neither
#231's four-tier gate nor #230's migration gate is cleared.

Subsequent fast-lane policy: `run` now skips the full independent verifier by
default and records `SKIPPED`; `run --verify` requests proof. The receipts
below predate that policy and retain their actual full-verification results.

## Identity and method

- Final measured source: `71d0dc3a86898a1179a720c1239ebd40a17a575d`,
  clean tree; product seal `df151a3be4e9da3ee0d441b711fa17a54657113ba0b323f594f1e67e3f5bfb7c`;
  harness seal `e4a5fb87132be0c5ae62a28d825a431d3ad6cfddb8a4f3b4cb1a0c5f2d4fee4d`;
  Cargo lock SHA-256 `cedd3b27d78d3dd9d5db1aa4d6d5a8036e308fbce09f5d8db645174d5e2296b9`.
  Archived binary SHA-256 values, per-case run IDs, Store paths, and producer
  namespaces are in `run.json`, `build.json`, and each `receipt.json`.
- Fixture profile `core-native-import-fixture-v2`, route
  `core-native-directory-import-v2`, seed 1. The 10,000 manifest SHA-256 is
  `c1d7937c9f90d3585558e6d20b35183a737530d386667d08badc3121a6b3d55e`.
  Prepared masters were reused only outside the timer and checked by seal.
  Source pages were not cold-invalidated or residency-qualified:
  `source-cache-uncontrolled-v1`, so `admission_eligible=false` in every row.
- Each selected case has one caller sample per exact source identity. The timer
  encloses one daemon-to-Service `ImportNativeDirectory` request, including
  source scan/read, C1/C2 construction and C5 genesis publication. A separate
  verifier process reopens Store and history, then checks every path, metadata
  item, byte count and SHA-256 through public readers. The verifier is never
  inside the performance timer. The 100,000 row has zero samples.
- Final command: `python3 core/benchmark/fs-bench-pro/runner.py run --family
  init_namespace --out benchmark-results/fs-bench-pro/final-init-spill-71d0dc3a-a`.
  Read-only evidence commands: `runner.py verify --run <run>` and
  `runner.py report --run <run>`; `verify` returned `PASS` for receipt custody,
  including the retained failing case. It does not mean the performance gate
  passed. The final family cycle was **19.582929042 s**, below the recommended
  30 s cycle. No second sample, best-of selection, or timeout change was used.

## Final per-case evidence

Times are one raw observation, not an average or percentile. The complete
command includes process lifecycle and cleanup. The fixed command budget is
15 s; the independent verifier budget is 5 s. No exception was declared.

| Case | Raw public Init | Complete command | Independent verifier | Telemetry | Cleanup | Result |
| --- | ---: | ---: | ---: | --- | --- | --- |
| `namespace-100-compact-v3` | 0.194627500 s | 0.261456375 s | 1.572469625 s, PASS | PASS | PASS | INELIGIBLE: cache uncontrolled |
| `namespace-1000-compact-v3` | 0.728781042 s | 0.788198625 s | 1.454934917 s, PASS | PASS | PASS | INELIGIBLE: cache uncontrolled |
| `namespace-10000` | 13.050897750 s | 15.120518875 s, `COMMAND_SLOW` | NOT_RUN: no confirmed root | INCOMPLETE | daemon and Service FAIL; stderr retained | FAIL |
| `namespace-100000` | — | — | — | — | — | NOT_RUN by first-pass registry; 2.7 s historical cold target remains unmet |

The 10,000-file public call returned `Unknown unknown=true` rather than a
`StackCreated` root. The Service was still active when its shutdown expired,
so its operation event and run summary were absent. Its process shared RSS
samples reached about 59 MB, but no phase memory peak is claimed. The retained
Store was 333,099,008 bytes; its uncommitted ordering directory held two run
files of 947 KiB and 768 KiB when the process was stopped. The history catalog
had zero published LayerStacks and one scope reservation. The retained run
files are hashed in the manifest as failure evidence. This receipt does not
show a product entry-count refusal; it shows unresolved work and a complete
command miss. C5 has no dedicated duration child, so none is inferred.

The machine snapshot listed Docker background processes. It is an observed
environment fact, not proof of a timed build or measurement overlap. External
child CPU and RSS are lifecycle-scoped; `rusage` lifetime peaks are not used
as phase gates. `telemetry.lft1` keeps daemon and Service namespaces and local
clocks separate, with no subtraction to invent transit time.

## Earlier immutable receipts

These are distinct source identities and remain in place. None is pooled
with the final rows or promoted to PASS. The first v1 route had no periodic
progress frame; the v2 route added one; the final source supplied the C1
ordering backing without changing the v2 fixture bytes or frozen limits.

| Source / output | 100 raw and proof | 1,000 raw and proof | 10,000 raw, complete command and failure | Family cycle |
| --- | --- | --- | --- | ---: |
| `0b98875c0369fb44699836f7809cc4a53cdf70a5` / `final-init-0b98875c-a` | 0.192077625 s; verifier PASS in 1.721793333 s; INELIGIBLE | 0.737358167 s; verifier PASS in 2.283988250 s; INELIGIBLE | 5.008934667 s; 7.099753958 s; fixed five-second no-progress limit; no verifier; FAIL | 14.405344667 s |
| `8dc25e4fcca700b90a0d61c84152c59c4acfb951` / `final-init-v2-8dc25e4-a` | 0.191578250 s; verifier PASS in 0.740696542 s; INELIGIBLE | 0.735526834 s; verifier PASS in 1.465283125 s; INELIGIBLE | 9.143564417 s; 9.202990666 s; C1 tree returned `Capacity` because the Service supplied no ordering backing; no verifier; FAIL | 14.650109833 s |

The v1 canary `final-canary-0b98875c-a` and v2 canary
`final-canary-v2-8dc25e4-a` each passed ten public-route witnesses, complete
telemetry, and cleanup. The earlier `final-canary-19c03aec7-a` was incomplete
because the harness expected readiness to be the first stderr line after
Service began emitting LFT1; `0b98875c0` fixed capture and retained that
failure. The final-source canary `final-canary-spill-71d0dc3a-a` again passed
all ten public witnesses and cleanup, but Service telemetry reported one
dropped record, so its canary status is **INCOMPLETE**. Its original capture
and zero-loss parser refusal are retained. There was no repeat to select a
cleaner canary.

## Build and checks

At the final product identity, the private-target first-use locked Cargo build
compiled 86 units in **14.63 s** (30 s maximum). The edited-product runner
build compiled one unit in **1.033777292 s**; the same private-target no-op
build was **0.10 s**. The final family reused archived binaries by exact seal,
recorded as `exact-binary-reuse`, and did not silently build during a sample.
Build logs are `cold-spill-cargo-build.log`, `noop-spill-cargo-build.log`, and
`final-canary-spill-71d0dc3a-a/build.log` under the result root.

The final-source checks passed: `cargo +1.85.1 test --manifest-path
core/Cargo.toml --locked --workspace`; `cargo +1.85.1 clippy
--manifest-path core/Cargo.toml --locked --workspace --all-targets -- -D warnings`;
`cargo +1.85.1 fmt --manifest-path core/Cargo.toml --all --check`;
`python3 core/tools/check_product_boundary.py` (261 production files); and
seven `python3 -m unittest discover -s core/benchmark/fs-bench-pro/tests -v`
tests. The focused 4,097-entry Service test passed in 0.90 s after a 3.11 s
compile and confirmed private ordering scratch removal. The full workspace
test log is `core-final-workspace-test-71d0dc3a.log` under the result root.
No root aggregate preflight or CI gate was run; neither exists for this tree.

## Remaining work

The current public importer has no default file or entry-count ceiling, and
the focused Service test crosses both the former 128-entry bootstrap limit and
the 4,096 pending-row threshold. The 10,000-file timed route still needs
bounded completion/progress within its frozen deadline and an independent
full verifier. The final canary needs a zero-loss receipt at a future relevant
identity or a separately diagnosed telemetry fix. Cold-source admission and
the 100,000-file tier require prospective contracts and fresh samples. The
retained failures cannot be relabelled or replaced by warmer reruns.
