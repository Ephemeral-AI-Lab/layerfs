[package]
name = "layerfs-sdk"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
layerfs-content = { path = "../layerfs-content" }
layerfs-layerstack-store = { path = "../layerfs-layerstack-store" }
layerfs-monitor = { path = "../layerfs-monitor" }
layerfs-workspace = { path = "../layerfs-workspace" }

[dev-dependencies]
layerfs-layerstack-store = { path = "../layerfs-layerstack-store", features = ["test-instrumentation"] }
