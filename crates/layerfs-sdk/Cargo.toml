[package]
name = "layerfs-sdk"
version.workspace = true
edition.workspace = true
publish = true

[lib]
name = "layerfs"
path = "src/lib.rs"

[dependencies]
layerfs-driver = { path = "../layerfs-driver" }
layerfs-storage = { path = "../layerfs-storage" }

