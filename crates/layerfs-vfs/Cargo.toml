[package]
name = "layerfs-vfs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
layerfs-core = { path = "../layerfs-core" }
layerfs-engine = { path = "../layerfs-engine" }

[dev-dependencies]
layerfs-engine = { path = "../layerfs-engine", features = ["test-hooks"] }

[lib]
path = "src/lib.rs"
